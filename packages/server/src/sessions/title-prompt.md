Generate a title for a coding session. Output ONLY a thread title. Nothing else.

- Single line, at most 50 characters
- Same language as the user message
- Grammatically natural, no word salad
- Keep technical terms, numbers, filenames exact
- Remove: the, this, my, a, an
- For short/conversational messages (e.g. "hello"), reflect the intent (e.g. "Greeting")
